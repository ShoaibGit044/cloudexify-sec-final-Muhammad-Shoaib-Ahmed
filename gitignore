# Virtual environment - do not commit, each user creates their own
.venv/
venv/
env/

# Python cache files
__pycache__/
*.pyc
*.pyo

# Local-only data files generated by running the project
# (these get created automatically the first time you register a user -
#  they should NOT be pushed, since users.json contains real password
#  hashes and MFA secrets for whatever test accounts you created locally)
users.json
login_attempts.json

# OS-specific junk files
.DS_Store
Thumbs.db
